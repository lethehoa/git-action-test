name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-22.04
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: multiple script
        run: |
          node -v
          npm -v
          mysql --version